// vector.cc -- shows how vector usage
#include <iostream>
#include <vector>
using namespace std;

int main(void)
{
    vector<int> vi; // 创建了一个空向量
    int n;
    cin >> n;
    vector<int> vd(n);
    return 0;
}