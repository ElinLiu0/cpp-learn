// rawsting.cc -- raw string
#include <iostream>
#include <string>

using namespace std;

int main(void){
    cout << R"(Jim "King" Tutt uses "\n" instead of endl.)" << endl;
    cout << R"+*("Who wouldn't?)",she whispered.)+*" << endl;
    return 0;
}